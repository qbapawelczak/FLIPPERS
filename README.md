# FLIPPERS

Project structure:

- `paper/` LaTeX sources (Overleaf or local)
- `code/` Python / Stata / R code
- `presentations/` Slides and talks
- `data/` Raw data (ignored by git)
- `misc/` Temporary holding area
